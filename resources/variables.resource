# Neste arquivo vamos definir todas a variáveis globais, ou seja, as variáveis 
# que serão utilizadas em todos os cenários que compõe este projeto.
*** Settings ***
Documentation       Global variables for using in all tests

*** Variables ***
${timeout}=  30
${long_timeout}=  60
${ulta_timeout}=  90

